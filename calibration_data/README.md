# 校准数据集说明

## 什么是校准数据集？

校准数据集用于 INT8 量化时，帮助 RKNN 工具确定每层网络的数值范围，从而实现更精确的量化。

## 如何准备校准数据集？

### 1. 选择图片
- 从训练数据集中选择 10-100 张**代表性图片**
- 图片应涵盖各种典型场景
- 确保图片质量良好，与实际应用场景相似

### 2. 放置图片
- 推荐将图片放在 `calibration_data/images/` 目录下
- 也可以使用其他任意路径

### 3. 编辑 calibration.txt
编辑 `calibration_data/calibration.txt` 文件，每行写一张图片的路径：

```
./calibration_data/images/img001.jpg
./calibration_data/images/img002.jpg
./calibration_data/images/img003.jpg
...
```

或使用绝对路径：
```
/mnt/e/datasets/my_dataset/train/image1.jpg
/mnt/e/datasets/my_dataset/train/image2.jpg
```

## 注意事项

- ⚠️ 如果选择 **INT8 量化模式**，必须提供有效的校准数据集
- ✓ 如果选择 **FP16 模式**，不需要校准数据集
- 💡 校准数据集不需要标注，只需要图片即可
- 📏 图片会被自动调整为模型输入尺寸，无需手动预处理

## 快速开始示例

如果你已有 `/mnt/e/rknn_model_zoo/examples/yolov8/` 下的校准数据，可以：

1. 复制现有的校准文件：
```bash
cp /mnt/e/rknn_model_zoo/examples/yolov8/firesmoke_calibration.txt ./calibration_data/calibration.txt
```

2. 或者创建软链接：
```bash
ln -s /mnt/e/rknn_model_zoo/datasets/COCO/val2017/ ./calibration_data/images
```

然后编辑 `calibration.txt` 指向这些图片。
